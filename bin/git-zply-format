#!/bin/bash
# Analagous to `git format-patch`
VERSION=0.1

function version() {
    echo $VERSION
    exit 0
}

function die() {
    >&2 echo $@
    exit 1
}

function usage() {
    die "usage: git zply-format [-h] [-o output-path] [-v] <since>"
}

OUTPUT_PATH=.

while getopts 'ho:v' opt; do
    case $opt in
        h) usage;;
        o) OUTPUT_PATH=$OPTARG;;
        v) version;;
        *) usage;;
    esac
done
shift $(($OPTIND - 1))

if [[ -z $1 ]]; then
    usage
fi

SINCE=$1

echo "Formatting patches..."
git format-patch -kpo $OUTPUT_PATH $SINCE > /dev/null || die "git format-patch failed"

for patch in `ls $OUTPUT_PATH/*.patch`; do
    git zply-fixup $patch > $patch.tmp || die "git zply-fixup failed"
    mv $patch.tmp $patch
done
