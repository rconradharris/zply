#!/bin/bash
CMD='git refresh-patches'
USAGE="usage: $CMD [-c] [-h] [-v] <patch-repo-dir> <since>"
VERSION=0.1

function die() {
    >&2 echo "[$CMD error]: $@"
    exit 2
}

function die_usage() {
    >&2 echo $USAGE
    exit 2
}

function help() {
    echo $USAGE
    exit 0
}

function version() {
    echo $VERSION
    exit 0
}

while getopts 'chv' opt; do
    case $opt in
        c) COLORIZE=1;;
        h) help;;
        v) version;;
        *) die_usage;;
    esac
done
shift $(($OPTIND - 1))

if [[ -z $1 ]] || [[ -z $2 ]]; then
    die_usage
fi

PATCH_REPO_DIR=$1
SINCE=$2
PATCHES_DIR=$PWD/.patches

git zply-format $PATCHES_DIR $SINCE || die "git zply-format failed"

if [[ -n $COLORIZE ]]; then
    COLOR_OPT=-c
fi

git zply-sync $COLOR_OPT $PATCHES_DIR $PATCH_REPO_DIR
SYNC_RET=$?
if [[ $SYNC_RET -gt 1 ]]; then
    die "git zply-sync failed"
fi
rm -rf $PATCHES_DIR
exit $SYNC_RET
