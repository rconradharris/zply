# TODO: Verify PATCH_REPO_PATH exists
# TODO: Verify PATCH_REPO_PATH is a git repository

- Add -h help to all commands

Better edge case handling:
    - Do not save if rebase in progress
    - Verify patch-repo is git repo and exists

- Add man pages for each command
- Add man git-zply for workflow documentation

Code cleanup:
- Better handle trailing-whitespace fixup (sed?)
